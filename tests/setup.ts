// Test setup file
// This runs before all tests

// Note: NODE_ENV cannot be reassigned in Node.js as it's read-only.
// Vitest handles test environment automatically. If you need to set
// a custom environment, use the 'environment' option in vitest.config.ts.

// Suppress console warnings during tests (optional)
// process.env.DEBUG = 'false';
